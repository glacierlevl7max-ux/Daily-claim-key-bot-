Rotate the bot token in BotFather before running.
Add bot as admin in the channel so getChatMember works.
Run:
1) pip install -r requirements.txt
2) copy .env.example to .env and paste new token
3) python bot.py
